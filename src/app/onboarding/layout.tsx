import type { Metadata } from 'next'
export const metadata: Metadata = {
  title: 'Hotel Onboarding - ProfitPro',
  description: 'Go live across 7 OTA platforms in 7 days. Full property audit, content, rates, and channel management handled end-to-end.',
}
export default function L({ children }: { children: React.ReactNode }) { return children }
