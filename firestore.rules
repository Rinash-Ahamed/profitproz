rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The application uses only the server-side Admin SDK. Admin access is controlled by IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
